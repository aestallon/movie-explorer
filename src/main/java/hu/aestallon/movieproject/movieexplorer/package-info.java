/**
 * Databases - Exercise 1
 *
 * <p>Create a movie catalogue using a database. Create the table structure for storing the movies' data. Implement CRUD operations:
 * <ul>
 * <li>Create new records based on user input.
 * <li>Allow querying based on multiple attributes (timeframe, category, director, etc.).
 * <li>Allow changing the data of a given movie based on user inputs.
 * <li>Let the user delete movies.
 * </ul>
 *
 * @author Szabolcs Bazil Papp
 */
package hu.aestallon.movieproject.movieexplorer;